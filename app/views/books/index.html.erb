<div>
  <%= flash[:notice] %>
</div>

<h1>Books</h1>

<body>

  <table class="table">

   <tr>
    <div class= "t-title">
      <th class="header1">Title</th>
    </div>

    <div class = "t-body">
      <th class="header2">Body</th>
    </div>
   </tr>

<% @books.each do |book| %>
    <tr>
      <td><%= book.title %></td>
        <td><%= book.body %>
    <div class="link">
      <a><%= link_to "Show", "/books/#{book.id}" %></a>
      <a><%= link_to "Edit"," /books/#{book.id}/edit"%></a>
      <a><%= link_to "Destroy", "books/#{book.id}", method: :delete, "data-confirm" => "Are you sure？" %></a>
      </div>
      </td>
    </tr>

<% end %>
</table>

<div class="form">
  <h2>New book</h2>

  <% if @book.errors.any? %>
  <%= @book.errors.count %>errors prohibited this book from being saved:
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

<%= form_with model: Book.new do |f| %>
  <div class= "label">
    <h4>Title</h4>
  <%= f.text_field :title %>
  </div>

  <div class= "label">
   <h4>Body</h4>
  <%= f.text_area :body %><br>
  </div>
  <br>
  <%= f.submit '  Create Book  ' %>
<% end %>
</div>
</body>